[metadata]
name = wine_admin
version = 0.1.0
description = WineAdmin monorepo package to allow editable installs during development

[options]
packages = find:
include_package_data = True

[options.packages.find]
where = .
