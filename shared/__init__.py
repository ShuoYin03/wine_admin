"""Shared utilities package for WineAdmin.

This package is intentionally minimal. Add modules here (for example:
- `text_utils.py`
- `db_utils.py`
- `constants.py`
)

After adding, install the repo in editable mode from the repo root:

    pip install -e .

Then other packages in this repo (e.g. `lwin_matcher`, `wine_spider`) can `import shared`.
"""

__all__ = []
